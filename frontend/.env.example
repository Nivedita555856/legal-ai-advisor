# Frontend environment variables
# For local dev, the Vite proxy handles /api → http://localhost:8000
# For production, set this to your Render backend URL
VITE_API_URL=https://your-backend.onrender.com/api
